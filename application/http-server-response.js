import BaseServerResponse from '../http-server/base-server-response.js';


/**
 * HttpServerResponse
 * ==================
 *
 * The HttpServerResponse class extends BaseServerResponse to provide a concrete
 * implementation for HTTP server responses within the Kixx framework. This class
 * can be extended in the future to add framework-specific response utilities,
 * but currently serves as a semantic alias for BaseServerResponse.
 */
export default class HttpServerResponse extends BaseServerResponse {
}
