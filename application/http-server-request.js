import BaseServerRequest from '../http-server/base-server-request.js';


/**
 * HttpServerRequest
 * =================
 *
 * The HttpServerRequest class extends BaseServerRequest to provide a concrete
 * implementation for HTTP server requests within the Kixx framework. This class
 * can be extended in the future to add framework-specific request utilities,
 * but currently serves as a semantic alias for BaseServerRequest.
 */
export default class HttpServerRequest extends BaseServerRequest {
}
